<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luosico.mapper.OrderMapper">

    <insert id="addOrder" useGeneratedKeys="true" keyProperty="orerId">
        insert into express_order
        (user_id,
         pay_id,
         courier_id,
         express_id,
         order_status)
        values (#{user_id},
                #{pay_id},
                #{courier_id},
                #{express_id},
                #{order_status})
    </insert>
    <select id="selectOrder" resultType="com.luosico.domain.Order">
        select (1)
        from express_order
        <where>
            <if test="order_id != null">
                and order_id = #{orderId}
            </if>
            <if test="user_id != null ">
                and user_id = #{userId}
            </if>
            <if test="pay_id != null ">
                and pay_id = #{payId}
            </if>
            <if test="courier_id != null ">
                and courier_id = #{courierId}
            </if>
            <if test="express_id != null ">
                and express_id = #{expressId}
            </if>
        </where>
    </select>
</mapper>