spring:
  application:
    name: redisApplication
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        enabled: true
        namespace: public
      config:
        server-addr: nacos:8848
        file-extension: yml
        group: DEFAULT_GROUP